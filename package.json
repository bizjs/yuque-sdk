{
  "name": "yuque-sdk",
  "version": "1.0.0",
  "description": "The open sdk for yuque.",
  "main": "index.js",
  "scripts": {
    "dev:client": "lerna run dev --stream --scope yuque-client",
    "dev:render": "lerna run dev --stream --scope yuque-doc-render",
    "build": "lerna run build --stream",
    "test": "cross-env DEBUG=yuque lerna run test --stream",
    "test:client": "cross-env DEBUG=yuque:client lerna run test --scope yuque-client --stream",
    "pub": "lerna run test && lerna run build && lerna publish"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/dotenv": "^8.2.0",
    "@types/jest": "^29.0.2",
    "cross-env": "^7.0.3",
    "dotenv": "^16.0.2",
    "father": "^4.0.4",
    "jest": "^29.0.3",
    "lerna": "^5.5.1",
    "nock": "^13.2.9",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "ts-jest": "^29.0.1",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.3"
  }
}
